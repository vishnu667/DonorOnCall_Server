name="Donor On Call Server"

server:{
  api:{
    apiServer:"Undertow"
    port:8080
    interface:"0.0.0.0"
  }
  mysql: {
    host = "localhost"
    port = 3306
    user = "root"
    password = "sam"
    db = "donor_on_call"
  }
}